    {{#each erros}}
  <div class="alert alert-error shadow-lg max-w-5xl mx-auto mb-4">
    <div>
      <span class="icon-[tabler--alert-circle] size-5"></span>
      <span>{{texto}}</span>
    </div>
  </div>
  {{else}}
{{/each}}
  
  <div class="min-h-screen  flex items-center justify-center ">
            <form class="form md:w-lg max-w-2xl grid grid-cols-1 items-center justify-center card p-10" action="/login" method="post">
                <div class="">

                    <!-- LOGOTIPO (IMAGEM) -->
                    <div class="cardtopo space-y-4 text-center flex flex-col items-center">
                        <img src="/img/logo.jpg" alt="Logotipo Empire Young" class="w-20 rounded-full border-2 border-slate-700 mb-1''''''''''''''''''''0">

                        <h2 class="titulo text-3xl">Iniciar Sessão</h2>
                        <p>Use o seu email para acessar a sua conta</p>
                    </div>

                    <div class="w-full space-y-5 mt-10">
                    <!-- EMAIL -->
                    <div class="cardgrupo">
                        <label>Email</label>
                        <input class="input" type="email" name="email" placeholder="Digite o seu email" required>
                    </div>

                    <!-- SENHA -->
                    <div class="">
                        <label>Senha</label>
                        <input class="input" type="password" id="senha" name="senha" placeholder="Digite a sua senha" required>
                        <span id="verSenha" class="ver-senha">Mostrar</span>
                    </div>

                    <!-- BOTÃO -->
                    <div class="cardgrupo botao text-center">
                        <button type="submit" class="btn btn-primary w-full">Acessar</button>
                        <p class="mt-3">Não tens uma conta? <a href="/cadastro" class="text-primary ">Cadastra-se aqui</a></p>
                    </div>

                </div>
                </div>
 </form>
</div>